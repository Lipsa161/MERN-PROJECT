// Greeting.js
import React from 'react';

// Define the Greeting component that accepts 'name' as a prop
const Greeting = ({ name }) => {
  return <h1>my-project, {name}!</h1>;
};

export default Greeting;
